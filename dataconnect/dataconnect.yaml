specVersion: "v1beta"
serviceId: "geaux-academy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "geaux-academy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
